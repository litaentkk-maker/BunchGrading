import sys
import os

# We need to find where RNS is installed.
# We can just write a python script that we execute with the system python? No, the system python doesn't have RNS installed. RNS is installed by Chaquopy during the Android build.
